<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Calculo_ductos_winUi_3.Views.QuoteSearchControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calculo_ductos_winUi_3.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border
        Width="350"
        MaxHeight="500"
        Padding="10">

            <StackPanel Spacing="8">

                <StackPanel Orientation="Horizontal" Spacing="6">

                    <TextBox Width="220"
                    PlaceholderText="Buscar..."
                    Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <ComboBox Width="100"
                    ItemsSource="{Binding FilterOptions}"
                    SelectedItem="{Binding SelectedFilter, Mode=TwoWay}" />

                </StackPanel>

                <ListView ItemsSource="{Binding FilteredQuotes}"
                IsItemClickEnabled="True"
                ItemClick="Quote_ItemClick"
                MaxHeight="300">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="{Binding PT}" />
                                <TextBlock Text="{Binding ExecutiveName}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
